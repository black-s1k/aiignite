# Copy to .env.local and fill in.
#
# The signup Google Form. NEXT_PUBLIC_* is inlined at build time, so this must
# be set before `next build` — changing it later requires a rebuild.
NEXT_PUBLIC_SIGNUP_URL=https://forms.gle/REPLACE_ME
